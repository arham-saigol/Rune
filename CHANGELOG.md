# Changelog

## Unreleased

- CLI commands renamed from `setup api` and `setup discord` to `setup-api` and `setup-discord`. The old space-separated forms (`setup api`, `setup discord`) are deprecated aliases and will be removed in a future release.
